import React from 'react';
import SideMenu from './Sidemenu';

const Header = () => {
    return (
        <header className="header">
            <SideMenu />
        </header>
    );
};

export default Header;
